# Eval Intake

This directory receives only an owner-safe eval-need packet after manual
trials show a repeated gap. The packet must name the invariant, owner,
consumer, manual cases, rejected existing surfaces, acceptance criteria,
privacy boundary, and why durable evaluation is now justified.

An intake packet is neither a suite nor a proof verdict.
