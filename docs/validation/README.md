# Validation Map

- `COMMAND_AUTHORITY.md` explains where executable sequences live.
- `VALIDATOR_TOPOLOGY.md` maps current validators to the narrow claims they
  protect.
- `SCRIPT_TOPOLOGY.md` maps active script organs and compatibility ingress.
- `validator_inventory.json` and `script_inventory.json` are compact current
  inventories, not historical ledgers.

Manual trials remain the authority for behavioral usefulness. These checks
only preserve explicitly named deterministic contracts.
