# AGENTS.md

## Applies to

This card applies to `legacy/` at the repository root.

## Role

Root `legacy/` keeps migration artifacts and temporary study notes that are still useful for provenance but no longer own active routes.

## Read before editing

Read root `AGENTS.md`, `DESIGN.md`, and the specific legacy artifact before using it as evidence.

## Boundaries

Do not revive legacy names, paths, or process language as active law without routing through the current owner surface. Temporary artifacts can inform decisions; they do not replace current cards, mechanics, or skill bundles.

## Validation

Run the validator for the active surface that receives any migrated meaning. If only legacy text changes, run `git diff --check`.

## Closeout

Report changed surfaces, checks run, checks skipped, remaining risk, and the next owner route. If a nearby source document carried agent-facing working law into this card, name that transfer.
