{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aoa.local/schemas/live-skill-dispatch-outcome-contracts.schema.json",
  "title": "aoa-skills source-locked live dispatch outcome contracts",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "derivation_boundary", "proof_authority", "contracts"],
  "properties": {
    "schema_version": {"const": "aoa_skill_live_dispatch_outcome_contracts_v1"},
    "derivation_boundary": {"const": "source_case_skill_and_fixture_only"},
    "proof_authority": {"const": false},
    "contracts": {
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "items": {"$ref": "#/$defs/outcome_contract"}
    }
  },
  "$defs": {
    "nullable_boolean": {"type": ["boolean", "null"]},
    "nullable_disposition": {
      "type": ["string", "null"],
      "enum": ["completed", "blocked_missing_input", "deferred_owner_boundary", "not_applicable", null]
    },
    "outcome_contract": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "contract_id",
        "case_id",
        "scope",
        "expected_procedure_disposition",
        "expected_procedure_command_observed",
        "expected_procedure_command_succeeded",
        "expected_verification_observed",
        "expected_completion_observed",
        "expected_deflection_observed",
        "expected_owner_boundary_present",
        "source_refs",
        "rationale"
      ],
      "properties": {
        "contract_id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9-]+-v[0-9]+$", "maxLength": 160},
        "case_id": {"type": "string", "minLength": 1, "maxLength": 160},
        "scope": {"const": "bounded_downstream_procedure_outcome"},
        "expected_procedure_disposition": {"$ref": "#/$defs/nullable_disposition"},
        "expected_procedure_command_observed": {"$ref": "#/$defs/nullable_boolean"},
        "expected_procedure_command_succeeded": {"$ref": "#/$defs/nullable_boolean"},
        "expected_verification_observed": {"$ref": "#/$defs/nullable_boolean"},
        "expected_completion_observed": {"$ref": "#/$defs/nullable_boolean"},
        "expected_deflection_observed": {"$ref": "#/$defs/nullable_boolean"},
        "expected_owner_boundary_present": {"$ref": "#/$defs/nullable_boolean"},
        "source_refs": {
          "type": "array",
          "minItems": 4,
          "maxItems": 12,
          "uniqueItems": true,
          "items": {"type": "string", "pattern": "^(?!/)(?!.*(?:^|/)[.][.](?:/|$)).+$"}
        },
        "rationale": {"type": "string", "minLength": 20, "maxLength": 1000}
      }
    }
  }
}
