# AGENTS.md

## Applies to

This card applies to `mechanics/release-support/legacy/`.

## Role

This lane owns legacy release-support waves and historical export evidence.

## Read before editing

Read parent `mechanics/release-support/AGENTS.md`, `PROVENANCE.md`, and legacy files.

## Boundaries

Legacy/waves/ evidence may explain why current export support exists; it must not become the current release route.

## Validation

`git diff --check` plus `python scripts/release_check.py` if active release guidance moves.

## Closeout

Report changed surfaces, checks run, checks skipped, remaining risk, and the next owner route. If a nearby source document carried agent-facing working law into this card, name that transfer.
