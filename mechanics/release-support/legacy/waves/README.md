# Release-Support Legacy Waves

This directory preserves older wave and phased-plan notes after active
release-support contracts moved into package-local `docs/`.

Use these files for historical accounting only. Start with
`../README.md` and `../../README.md` for current active routes.
