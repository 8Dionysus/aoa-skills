# Release-Support Examples

Public-safe examples for export refresh, install config snippets, trust-store
hints, allowlists, and runtime guardrail configuration.

These examples are local adapter scaffolds only. They do not contain secrets,
workspace-private paths, or activation permission.
