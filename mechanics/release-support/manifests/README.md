# Artifact Bundle Manifest

`release_manifest.bundle.json` adapts the generated AoA Skills release manifest
to the external `abyss-machine` artifact bundle contract. The external owner
performs signing, registry, subject-store, and consumer trust admission; this
repository only supplies the public-safe subject and adapter metadata.
