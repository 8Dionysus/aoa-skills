# Release Support Roadmap

Next honest release-support passes:

- verify all generated release-manifest paths stay fresh after the rehome
- decide whether release-support needs a compact generated index
- keep historical wave docs in legacy unless an active contract still depends
  on one directly
