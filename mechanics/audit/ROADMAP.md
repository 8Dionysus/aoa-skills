# Audit Roadmap

Next honest audit passes:

- keep review records in `docs/reviews/` unless a later source-of-truth pass
  proves they should move
- tighten generated/public readout references after the first full release
  check on the rehomed paths
- split proof-facing follow-up to `aoa-evals` when audit evidence becomes a
  public claim
