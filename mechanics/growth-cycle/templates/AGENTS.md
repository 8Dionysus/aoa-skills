# AGENTS.md

## Applies to

This card applies to `mechanics/growth-cycle/templates/`.

## Role

This lane owns Growth-cycle templates for session-harvest evidence objects.

## Read before editing

Read parent `mechanics/growth-cycle/AGENTS.md`, `PARTS.md`, the consuming
session-harvest surface, and at least one current harvest note.

## Boundaries

Templates define source shape only. They do not promote a candidate, create a
skill, prove a claim, write memory, or override the active Growth-cycle route.

## Validation

Full lane command sequences live in `config/validation_lanes.json`; this local card may name only focused owner checks, lane ids, or the nearest route for the changed surface.

Run `git diff --check` plus parent validation if the template changes active
harvest-note expectations.

## Closeout

Report changed surfaces, checks run, checks skipped, remaining risk, and the
next owner route. If a nearby source document carried agent-facing working law
into this card, name that transfer.
