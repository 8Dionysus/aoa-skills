# AGENTS.md

## Applies to

This card applies to `mechanics/` unless a nearer package card narrows it.

## Role

`mechanics/` holds only three owner-local supporting systems that remain
independently durable: requested Agon candidates, Questbook obligations, and
portable release/pack handoff.

## Read before editing

Inspect the target package card and source. Use the README to choose a package;
use `ARTIFACT_TOPOLOGY.md` only when inventory, ownership, or flow moves.

## Boundaries

Mechanics do not own capability meaning, callable procedures, session traces,
proof, retrieval, runtime state, or another repository's workflow. When a
supporting package loses an independent owner contract, remove it rather than
preserving a ceremonial layer.

## Validation

Use the target package's focused manual and deterministic checks. Run broader
lanes only when a shared projection or release surface moved.

## Closeout

Report the independent contract preserved, package artifacts changed or
retired, manual case, checks, and stronger owner route.
