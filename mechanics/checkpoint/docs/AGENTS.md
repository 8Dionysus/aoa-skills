# AGENTS.md

## Applies to

This card applies to `mechanics/checkpoint/docs/`.

## Role

This lane owns checkpoint documentation for closeout continuity and checkpoint-note shape.

## Read before editing

Read parent `mechanics/checkpoint/AGENTS.md`, `mechanics/checkpoint/docs/README.md`, and checkpoint tests before editing.

## Boundaries

Do not let checkpoint language become memory authority, proof authority, or hidden completion state.

## Validation

Full lane command sequences live in `config/validation_lanes.json`; this local card may name only focused owner checks, lane ids, or the nearest route for the changed surface.

`python -m pytest -q tests/test_session_checkpoint_note.py tests/test_session_growth_kernel_maturity.py`.

## Closeout

Report changed surfaces, checks run, checks skipped, remaining risk, and the next owner route. If a nearby source document carried agent-facing working law into this card, name that transfer.
