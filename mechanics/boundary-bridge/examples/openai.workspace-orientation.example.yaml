# Example generated Codex-facing openai.yaml scaffold.
# slug: workspace-orientation
# Do not copy this into skills/*/agents/openai.yaml unchanged.
# In aoa-skills, named MCP dependencies belong in config/openai_skill_extensions.json
# and then flow into the generated export under .agents/skills/*/agents/openai.yaml.
interface:
  display_name: AoA workspace orientation
  short_description: Orient within the sibling workspace and pick the strongest owner
    surface before deeper work.
  default_prompt: Use this route to orient within the AoA workspace, map the owning
    repository and strongest source surfaces, and hand off only after route clarity
    is explicit.
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
  - type: mcp
    value: aoa_workspace
    description: AoA workspace orientation MCP server
    transport: stdio
