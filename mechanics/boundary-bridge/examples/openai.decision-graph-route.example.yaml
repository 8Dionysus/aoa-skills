# Example generated Codex-facing openai.yaml scaffold.
# slug: decision-graph-route
# Do not copy this into skills/**/agents/openai.yaml unchanged.
# In aoa-skills, named MCP dependencies belong in config/openai_skill_extensions.json
# and then flow into the generated export under .agents/skills/*/agents/openai.yaml.
interface:
  display_name: AoA decision graph route
  short_description: Use the workspace decision graph to find, create, or correct
    decision-lane records while preserving repo-local source truth.
  default_prompt: Use this route to query the AoA decision graph first, then verify
    or update repo-local docs/decisions source records through the owning repository.
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
  - type: mcp
    value: aoa_decisions
    description: AoA workspace decision graph MCP server
    transport: stdio
