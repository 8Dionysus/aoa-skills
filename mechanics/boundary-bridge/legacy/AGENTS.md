# AGENTS.md

## Applies to

This card applies to `mechanics/boundary-bridge/legacy/`.

## Role

This lane owns retired Boundary-bridge fragments and strategy provenance for
skill selection, registry, router, and downstream integration boundaries.

## Read before editing

Read parent `mechanics/boundary-bridge/AGENTS.md`, `PROVENANCE.md`, and
`legacy/README.md`.

## Boundaries

Legacy evidence may explain current Boundary-bridge choices; it must not become
the current registry, router, overlay, adapter, or downstream authority.

## Validation

Run `git diff --check` plus parent validation if legacy meaning is migrated.

## Closeout

Report changed surfaces, checks run, checks skipped, remaining risk, and the
next owner route. If a nearby source document carried agent-facing working law
into this card, name that transfer.
