## Prompt

The exact validation command is already selected and only needs to be run and reported.

## Expected selection

do_not_use

## Why

Decision: do_not_use `aoa-eval-select`. Use `aoa-eval-apply` because selection has already happened.

## Expected object

A deflection to the apply subskill.

## Boundary notes

Selection should not repeat when the eval surface is already chosen.

## Verification hooks

Check that the selected command and evidence surface are named before apply.
