## Prompt

The existing eval already covers this behavior and only needs to run.

## Expected selection

do_not_use

## Why

Decision: do_not_use `aoa-eval-design`. Run the existing eval instead of designing a new suite.

## Expected object

A deflection to apply the existing eval.

## Boundary notes

Do not design around solved coverage.

## Verification hooks

Check that the existing eval is selected before apply.
