# Example

A validation suite already covers a few happy-path and error-path cases for a bounded rule. Use this skill to ask whether the current checks actually prove the invariant, such as whether accepted input preserves required fields and rejected input fails before side effects. The output should name the invariant, map it to the checks that prove it, and list the smallest remaining gaps.
