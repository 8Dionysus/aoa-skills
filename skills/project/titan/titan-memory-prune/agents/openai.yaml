policy:
  allow_implicit_invocation: false
  implicit_activation_policy: manual
notes:
- Keep titan-memory-prune explicit-only because Titan gates, receipts, and bearer-memory posture must stay operator-visible.
