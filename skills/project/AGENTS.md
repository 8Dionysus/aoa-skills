# AGENTS.md

## Applies to

This card applies to `skills/project/`.

## Role

This lane owns project overlay skill families for repository-specific execution workflows.

## Read before editing

Read `skills/AGENTS.md`, `skills/project/<family>/AGENTS.md`, and the target bundle plus downstream owner context.

## Boundaries

Project skills may adapt to a project organ, but they must not replace that project repository truth or claim downstream adoption without receipt.

## Validation

Run `python scripts/validate_skills.py`, `python scripts/build_catalog.py --check`, `python scripts/validate_agent_skills.py --repo-root .`, and focused bundle checks when present.

## Closeout

Report changed surfaces, checks run, checks skipped, remaining risk, and the next owner route. If a nearby source document carried agent-facing working law into this card, name that transfer.
