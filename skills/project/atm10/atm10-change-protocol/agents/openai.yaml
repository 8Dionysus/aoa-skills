policy:
  allow_implicit_invocation: false
  implicit_activation_policy: manual
notes:
- This thin atm10 overlay stays explicit-preferred while local repo paths, commands, review checklists, and approval notes still need review.
