policy:
  allow_implicit_invocation: false
  implicit_activation_policy: manual
notes:
- This scaffold is explicit-only because it classifies authority and execution boundaries for potentially sensitive actions.
