policy:
  allow_implicit_invocation: true
  implicit_activation_policy: invoke
notes:
- This router may be invoked implicitly for clear decision-lane work.
- After classification, the router must fully read exactly one selected subskill before that child performs graph lookup or a write; root-only execution is incomplete.
