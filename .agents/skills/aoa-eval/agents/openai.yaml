interface:
  display_name: AoA Eval
  short_description: Route AoA eval-lane work
  default_prompt: Use $aoa-eval to select one eval route, inspect existing surfaces first, and keep central proof, local intake, MCP access, and session evidence separate.
  icon_small: ./assets/small-logo.svg
  icon_large: ./assets/large-logo.svg
  brand_color: '#2563EB'
policy:
  implicit_activation_policy: invoke
  allow_implicit_invocation: true
