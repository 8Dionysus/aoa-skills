# Skill Applicability Map

## Session

- task:
- current owner repo:
- touched surfaces:
- main risk:

## Ring or candidate set

- ring name or family:
- phase under review: `execution`, `closeout`, or `harvest`

## Applicability table

| skill | status | reason | next honest move |
|---|---|---|---|
| `skill-name` | `apply_now` | why this skill belongs to the current plane | what closes after it |
| `skill-name` | `defer` | why it belongs later rather than now | what must happen first |
| `skill-name` | `skip` | why it does not match this task | what layer owns the concern instead |

Allowed statuses:

- `apply_now`
- `defer`
- `skip`

## Apply-now set

- `skill-name`

## Deferred set

- `skill-name` - reason:

## Explicit skips

- `skill-name` - reason:

## Harvest check

- reusable candidates detected:
- owner-layer ambiguity detected:
- repeated route detected:
- next plane:

## Notes

- keep this bounded
- do not confuse `defer` with `skip`
- do not treat the map itself as a harvest verdict
